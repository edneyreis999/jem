export enum SUB_GOLD_EVENTS {
  /**
   * @from: 'CoreScene',
   * @to: 'HUDScene',
   * @description: 'Used to display player gold in HUD',
   */
  PLAYER_UPDATE_GOLD = "player.update.gold",
}

export enum PUB_GOLD_EVENTS {}
